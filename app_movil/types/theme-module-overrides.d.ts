declare module '../../constants/theme' {
  export const Colors: {
    light: { icon: string };
    dark: { icon: string };
    [key: string]: any;
  };
}

declare module '*constants/theme' {
  export const Colors: {
    light: { icon: string };
    dark: { icon: string };
    [key: string]: any;
  };
}
